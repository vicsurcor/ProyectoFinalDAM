@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @Html.ActionLink("Login", "Login", "Account", null, new { @class = "btn btn-primary btn-lg" })
    @Html.ActionLink("Register", "Register", "Account", null, new { @class = "btn btn-secondary btn-lg" })
</div>
